def send_notifications(a, b):
    print('awd', flush=True)
    # cookies = RequestsCookieJar()
    # cookies.set('a', a)
    # cookies.set('b', b)

    # api = faapi.FAAPI(cookies=cookies)
    # user = api.me()
    # notifications = user.user_page.find('li', {'class': 'message-bar-desktop'})
    
    # notification_text = []
    # for notification in notifications.children:
    #     text = notification.get_text().strip()
    #     if text:
    #         notification_text.append(text)

    # return None
